#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PAYLOAD="${1:?usage: oc-github-project-normalize <payload.json>}"
bash "$ROOT/scripts/oc-sdlc" normalize-webhook "$PAYLOAD"
