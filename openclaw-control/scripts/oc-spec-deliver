#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
APPROVAL_ID="${1:?usage: oc-spec-deliver <approval-id>}"
bash "$ROOT/scripts/oc-approval" deliver-spec "$APPROVAL_ID"
