#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PAYLOAD="${1:?usage: oc-spec-publish <payload.json>}"
bash "$ROOT/scripts/oc-sdlc" publish-request "$PAYLOAD"
