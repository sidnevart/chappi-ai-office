#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PAYLOAD="${1:?usage: oc-alert-route <payload.json>}"
bash "$ROOT/scripts/oc-hook-run" alert-route "$PAYLOAD"
