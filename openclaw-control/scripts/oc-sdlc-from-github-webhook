#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PAYLOAD="${1:?usage: oc-sdlc-from-github-webhook <payload.json>}"
bash "$ROOT/scripts/oc-sdlc" from-webhook "$PAYLOAD"
